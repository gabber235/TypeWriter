// Shared color value. The canonical representation is one ARGB 32 bit value.
// Consumers convert the signed wire integer to an unsigned 32 bit color without changing its bits.
struct Color {
  argb: int32 = 0;
}
