<SYSTEM>This is the full developer documentation for Typewriter</SYSTEM>

# Home

> Typewriter is a free Paper plugin plus a web panel for building NPC dialogues, quests, scenes and living NPCs without code, and for extending them in Kotlin.

# TYPEWRITER

NEXT GEN PLAYER INTERACTIONS

[Get Started](/branches/features/v1-docs/docs/)[Join Discord](https://discord.gg/HtbKyuDDBw)

# TYPEWRITER

NEXT GEN PLAYER INTERACTIONS

[Get Started](/branches/features/v1-docs/docs/)[Join Discord](https://discord.gg/HtbKyuDDBw)

What you build

## Dialogue, quests, scenes and NPCs

Each one is a page in the panel. Pick a page type, place entries on it, connect them.

* Dialogue

  NPCs talk in chat, players answer with options, and each answer can lead somewhere else.

  ![Wireframe of a sequence page: an event element connected to dialogue and action elements, with the inspector open.](/branches/features/v1-docs/_astro/panel-pages.JptuZROP_Z2fBhTV.webp)

* Quests

  Objectives count kills, distance and blocks, show progress in the sidebar, and hand over to the next objective when done.

  ![Wireframe of a manifest page: a tree of audience elements that stay active while their condition holds.](/branches/features/v1-docs/_astro/panel-manifest.BD05d5Bt_MMsYi.webp)

* Scenes

  Camera moves, titles, particles and spoken lines on one timeline, timed to the frame.

  ![Wireframe of the scene editor: entry tracks with cue spans and keyframes on a frame ruler.](/branches/features/v1-docs/_astro/panel-scene.XOtH-nf3_Z1tLUBy.webp)

* NPCs

  Define an NPC once, then place instances that patrol a road network, look at players and react when clicked.

  ![Wireframe of a selected element next to its inspector fields.](/branches/features/v1-docs/_astro/panel-inspector.CdPABOSh_1PfcqD.webp)

How it works

## Three steps from a fresh server to a talking NPC

1. 1

   ### Step 1: Connect your server to a realm

   Install the plugin next to PacketEvents and start the server. The console prints a ten-character registration token; paste it into the panel under **Services → Connect a Service** and the server joins your [realm](/branches/features/v1-docs/glossary/#realm).

   ![Wireframe mockup of the panel’s Services page with the Connect a Service dialog open: a ten-cell token field with seven cells filled, and a Connect button.](/branches/features/v1-docs/_astro/panel-connect.-y9hNgch_Z2wl2re.webp)

2. 2

   ### Step 2: Build pages in the panel

   Create a [book](/branches/features/v1-docs/glossary/#book) in the Library, add [pages](/branches/features/v1-docs/glossary/#page), and place entries on them. Sequence pages hold dialogue and actions, scene pages hold timed cues, manifest pages hold audiences that stay active while a condition is true.

   ![Wireframe mockup of the Library: a grid of eight coloured book covers, the first one selected, with its details in the inspector.](/branches/features/v1-docs/_astro/panel-library.GSCa5z4k_1l8wGm.webp)

3. 3

   ### Step 3: Play it on your server

   Players talk to your NPCs in chat, pick options, complete objectives and watch scenes on the server connected to the realm. Nothing to install on the client.

   Illustration

   Blacksmith: You look like someone who can swing a hammer. Want work?

   1▸ What do you need done?

   2Not today.

[Start with the install guide →](/branches/features/v1-docs/docs/)

The panel

## One editor for graphs and timelines

Sign in, open your realm, and every book, page and entry is a click away. Nothing to install. Click a pin to see what each part of the editor does.

![Wireframe mockup of the page editor: page tree on the left, a graph of connected elements in the middle, the inspector on the right.](/branches/features/v1-docs/_astro/panel-pages.JptuZROP_Z2fBhTV.webp)1234

The page editor for a sequence page

1. 1

   **Pages by chapter** — every page in the book, grouped into chapters. The search box above filters them.
2. 2

   **Event element** — something the player did. Its arrows are [triggers](/branches/features/v1-docs/glossary/#trigger): when this finishes, the next element runs.
3. 3

   **Inspector** — the fields of the selected element, rendered from the extension’s definition.
4. 4

   **Shortcuts** — the panel is keyboard first. `⇧ ShiftShift+M` moves, `⇧ ShiftShift+R` resizes, `CtrlControl+H` to `CtrlControl+L` switch panes, `EscEscape` returns to Normal mode.

* ![Wireframe mockup of the scene editor: entry tracks on the left, cue spans and keyframe diamonds along a frame ruler, the cue inspector on the right.](/branches/features/v1-docs/_astro/panel-scene.XOtH-nf3_Z1tLUBy.webp)

  ### Scenes on a timeline

  Scene pages swap the graph for a timeline: one track per entry, [cues](/branches/features/v1-docs/glossary/#cue) placed by frame, twenty frames per second.

* ![Wireframe mockup of the search palette: a query with a selector chip, filter chips and five colour-coded results.](/branches/features/v1-docs/_astro/panel-search.C_1P4u0m_2qEt7o.webp)

  ### Search with selectors

  Press `S` or `/` and search the whole realm with `key:value` selectors. Results group into entries, definitions, pages, books and tags.

* ![Wireframe mockup of the page editor in the dark theme.](/branches/features/v1-docs/_astro/panel-pages.JptuZROP_Z2fBhTV.webp)Dark

  ![Wireframe mockup of the page editor in the light theme.](/branches/features/v1-docs/_astro/panel-pages-light.CiHqWTG__lquN4.webp)Light

  55

  ### Dark or light

  The panel follows your system theme, and so do these docs. Drag the handle to compare.

Player memory

## Stories that remember each player

Every player gets their own version of your story. One page can greet a stranger, thank the person who brought the iron and turn away someone who already claimed the reward. Typewriter stores [facts](/branches/features/v1-docs/glossary/#fact) per player; [criteria](/branches/features/v1-docs/glossary/#criteria) read them before an entry runs and [modifiers](/branches/features/v1-docs/glossary/#modifier) change them afterwards.

Script

One page in the panel, played three times.

Player file

What Typewriter has stored so far. **Purple** changed in this beat.

1. ### Day 1 · The forge

   Blacksmith

   You look like someone who can swing a hammer. Bring me five iron ingots and there's coin in it for you.

   Player

   I'll find them.

   Player file

   * Iron job

     started, changed

   * Ingots handed in

     0 of 5, changed

2. ### Day 1 · Later

   Blacksmith

   Five ingots, as promised. Here, take these coins and get yourself a warm meal.

   Player

   Pleasure doing business.

   Player file

   * Iron job

     done, changed

   * Ingots handed in

     5 of 5, changed

   * Reward paid

     today, changed

3. ### Day 2 · The forge

   Blacksmith

   Back already? The forge is stocked until tomorrow. Try the notice board by the market.

   Player

   Until tomorrow, then.

   Player file

   * Iron job

     done

   * Reward paid

     yesterday, changed

   * Next job in

     6 h, changed

### What it can remember

Each is a fact entry you add once, the purple ones in the panel. Typewriter keeps the values per player, per world or for the whole server, and saves them for you.

* Forever

  Joined before, finished the quest, took the reward: kept until you reset it.

* Until logout

  Time played this session, warnings given tonight: gone when the player leaves.

* For a set time

  The guard stays angry for twenty minutes, then forgets about it.

* Counting down

  Daily rewards and cooldowns keep ticking while the player is offline.

* On a schedule

  Weekly resets and shop opening hours, cleared by a cron expression.

* Read from the world

  Items in the inventory, balance, permissions, WorldGuard region: looked up live, nothing stored.

For developers

## Every entry is a Kotlin class

ExampleActionEntry.kt

```kotlin
@Entry("example_action", "An example action entry.", Colors.RED, "material-symbols:touch-app-rounded")
class ExampleActionEntry(
    override val name: String = "",
) : ActionEntry {
    override fun ActionTrigger.execute() {
        // Do something with the player
    }
}
```

Show the whole classHide the whole class

ExampleActionEntry.kt

```kotlin
package com.example.myextension


@Entry("example_action", "An example action entry.", Colors.RED, "material-symbols:touch-app-rounded")
class ExampleActionEntry(


    override val id: String = "",
    override val name: String = "",
    override val criteria: List<Criteria> = emptyList(),
    override val modifiers: List<Modifier> = emptyList(),
    override val triggers: List<Ref<TriggerableEntry>> = emptyList(),
) : ActionEntry {


    override fun ActionTrigger.execute() {
        // Do something with the player
    }
}
```

ExampleActionEntry.kt

```kotlin
package com.example.myextension


@Entry("example_action", "An example action entry.", Colors.RED, "material-symbols:touch-app-rounded")
class ExampleActionEntry(


    override val id: String = "",
    override val name: String = "",
    override val criteria: List<Criteria> = emptyList(),
    override val modifiers: List<Modifier> = emptyList(),
    override val triggers: List<Ref<TriggerableEntry>> = emptyList(),
) : ActionEntry {


    override fun ActionTrigger.execute() {
        // Do something with the player
    }
}
```

An [extension](/branches/features/v1-docs/glossary/#extension) is a JAR of Kotlin classes marked `@Entry`. The panel reads the annotation for the name, description, colour and icon, turns the constructor parameters into inspector fields, and the server runs the body. The ten extensions that ship with Typewriter, about five hundred entries, are written exactly like this one.

Ships with

* Basic

  190 entries

* Entity

  235 entries

* Quest

  24 entries

* RoadNetwork

  11 entries

Plus integrations for Vault, WorldGuard, Citizens, MythicMobs, RPGRegions, and SuperiorSkyblock.

[Read the develop docs](/branches/features/v1-docs/develop/)

Before you start

## Runs on a plain Paper server

Drop the plugin into plugins/ next to PacketEvents and start the server. Folia is not supported.

* Server

  Paper 1.21

* Runtime

  Java 21

* Dependency

  [PacketEvents](https://modrinth.com/plugin/packetevents/versions?l=paper)

* Players install

  Nothing

* Panel

  Hosted

* Price

  Free

Questions

Do I need to know how to code?

No. Everything on this page is built in the panel by placing entries and connecting them. Kotlin only comes in when you write your own extension because the shipped entries do not cover a case.

Is 1.0 ready to use?

Not yet. Typewriter 1.0 is in active development: the new panel, the realm service and these docs are being built in the open. Expect gaps, and ask on the [Discord](https://discord.gg/HtbKyuDDBw) for the current state before moving a live server to it.

Do I host the panel?

No. The 1.0 panel is a hosted web app. You sign in, create an organization and a realm, and connect servers to it with a registration token. The plugin is the only thing you run.

Is it open source?

It is source-available. The code is on [GitHub](https://github.com/Gabber235/typewriter) under the Typewriter Software License, which allows use and contribution but not redistribution or modification outside the project.

Anything else

Questions get answered on the [Discord](https://discord.gg/HtbKyuDDBw); bugs and feature requests go to [GitHub issues](https://github.com/Gabber235/typewriter/issues).

## Your first talking NPC is an evening's work.

Install the plugin, connect a realm and build your first page. The docs start where this page ends.

[Get started](/branches/features/v1-docs/docs/)[Join the Discord](https://discord.gg/HtbKyuDDBw)

Development is supported by 15 sponsors

* [![](https://github.com/myiume.png?size=96)](https://github.com/myiume)
* [![](https://github.com/iamyellowhead.png?size=96)](https://github.com/iamyellowhead)
* [![](https://github.com/RenaudRl.png?size=96)](https://github.com/RenaudRl)
* [![](https://github.com/ItsJustJar.png?size=96)](https://github.com/ItsJustJar)
* [![](https://github.com/GuavaDealer.png?size=96)](https://github.com/GuavaDealer)
* [![](https://github.com/artie-mortus.png?size=96)](https://github.com/artie-mortus)
* [![](https://github.com/Chaosgh.png?size=72)](https://github.com/Chaosgh)
* [![](https://github.com/Junkehh.png?size=72)](https://github.com/Junkehh)
* [![](https://github.com/vadim-soude.png?size=72)](https://github.com/vadim-soude)
* [![](https://github.com/rafael5gr2.png?size=72)](https://github.com/rafael5gr2)
* [![](https://github.com/Hinogo2210.png?size=72)](https://github.com/Hinogo2210)
* [![](https://github.com/atcpybd.png?size=72)](https://github.com/atcpybd)
* [![](https://github.com/Tanerx.png?size=72)](https://github.com/Tanerx)
* [![](https://github.com/lilZoey.png?size=72)](https://github.com/lilZoey)

[Sponsor the project](https://github.com/sponsors/gabber235)

# Develop

> Developer documentation for Typewriter 1.0. Nothing is written yet; this page marks the section.

TL;DR

This section will explain how to build a Typewriter [extension](/branches/features/v1-docs/glossary/#extension) in Kotlin: project setup with the module plugin, the [entry](/branches/features/v1-docs/glossary/#entry) families, [interactions](/branches/features/v1-docs/glossary/#interaction), querying, triggering and dependency injection.

## What will live here

[Section titled “What will live here”](#what-will-live-here)

* **Extensions**: what an extension is and what it can add to the engine.
* **Getting started**: a Gradle project with the module plugin, building the JAR and loading it with /tw reload.
* **Entries**: trigger, static, manifest and cinematic entries (the code name for [scene](/branches/features/v1-docs/glossary/#scene) cues), with a guide for picking the right base type.
* **Engine APIs**: interactions, querying, triggering and dependency injection.

## Before you begin

[Section titled “Before you begin”](#before-you-begin)

* JDK 21 or newer and an IDE with Kotlin support.
* Working knowledge of Gradle and the Paper API.
* Typewriter 1.0.2 on a Paper 1.21 test server.

Pages in this section use the same [writing syntax](/branches/features/v1-docs/docs/01-syntax/) as the user documentation.

# Documentation

> The user documentation for Typewriter 1.0. Nothing is written yet; the syntax reference shows how pages will be built.

TL;DR

Typewriter 1.0 is being documented from scratch. This section will cover installing the plugin, working in the web panel and building stories. For now it holds the [writing syntax](/branches/features/v1-docs/docs/01-syntax/) reference that every future page is built with.

## What will live here

[Section titled “What will live here”](#what-will-live-here)

* **Getting started**: installing the plugin, connecting your server to a [realm](/branches/features/v1-docs/glossary/#realm), the panel layout.
* **Creating stories**: interactions, [scenes](/branches/features/v1-docs/glossary/#scene), facts, entities, quests and road networks, as step-by-step tutorials.
* **Concepts**: [organizations](/branches/features/v1-docs/glossary/#organization), [books](/branches/features/v1-docs/glossary/#book), [pages](/branches/features/v1-docs/glossary/#page), [entries](/branches/features/v1-docs/glossary/#entry), [triggers](/branches/features/v1-docs/glossary/#trigger), [facts](/branches/features/v1-docs/glossary/#fact) and [criteria](/branches/features/v1-docs/glossary/#criteria) explained once, properly.
* **Helpful features** and **troubleshooting**.

## Writing a page

[Section titled “Writing a page”](#writing-a-page)

Every page is Markdown with a small set of custom directives on top: keycaps, copyable commands, site variables, glossary terms, collapsibles, gated sections, decision wizards, annotated screenshots and comparison sliders. The [syntax reference](/branches/features/v1-docs/docs/01-syntax/) shows each one with its source and the rendered result on a single page.

Building extensions instead? Head to the [developer documentation](/branches/features/v1-docs/develop/).

# Writing Syntax

> Every authoring feature of this site on one page, each with its source and the rendered result.

TL;DR

Pages are Markdown plus a handful of directives. Inline ones look like `:name[text]`, block ones open with `:::name` and close with `:::`. Components such as Steps, Tabs and the video player are imported in `.mdx` files only.

## Page basics

[Section titled “Page basics”](#page-basics)

Every page starts with frontmatter. `title` and `description` are required; `badge` is optional and shows next to the page title in the sidebar.

```md
---
title: Writing Syntax
description: Every authoring feature on one page.
badge: new
---
```

Headings start at `##`. The title comes from the frontmatter, so never write a `#` heading. Use `.md` unless you import a component; then use `.mdx`.

## Inline text

[Section titled “Inline text”](#inline-text)

### Keyboard keys

[Section titled “Keyboard keys”](#keyboard-keys)

`+` joins keys pressed together, `,` separates keys pressed one after another. Names like `cmd`, `shift`, `enter` and `up` get their symbols.

```md
Press :kbd[Shift+M] to enter Move mode, or :kbd[Ctrl+H, Ctrl+L] to hop between panes.
```

Press `⇧ ShiftShift+M` to enter Move mode, or `CtrlControl+HthenCtrlControl+L` to hop between panes.

### Commands

[Section titled “Commands”](#commands)

A command chip copies itself when clicked. Add `{nocopy}` for a plain chip.

```md
Run :cmd[/tw reload] after editing. Read-only: :cmd[/tw version]{nocopy}.
```

Run /tw reload after editing. Read-only: /tw version.

### Variables

[Section titled “Variables”](#variables)

Values come from `src/content/variables.yml`, so a version bump is one edit. They work in text, in bold, and inside link URLs.

```md
Typewriter :var[latest] needs Java :var[java]. Join the [Discord](:var[discord]).
```

Typewriter 1.0.2 needs Java 21. Join the [Discord](https://discord.gg/HtbKyuDDBw).

### Glossary terms

[Section titled “Glossary terms”](#glossary-terms)

Terms are never linked automatically. Mark the first meaningful mention on a page; the reader gets a hover card. Use `as=` when the wording differs from the glossary aliases.

```md
An :term[entry] lives on a :term[page] inside a :term[book]. Every :term[node on the graph]{as=page-element} is one.
```

An [entry](/branches/features/v1-docs/glossary/#entry) lives on a [page](/branches/features/v1-docs/glossary/#page) inside a [book](/branches/features/v1-docs/glossary/#book). Every [node on the graph](/branches/features/v1-docs/glossary/#page-element) is one.

### Highlight

[Section titled “Highlight”](#highlight)

```md
Only the ==first trigger== fires.
```

Only the *first trigger* fires.

## Callouts

[Section titled “Callouts”](#callouts)

Asides take a variant name and an optional title in brackets. Variants: `info`, `warning`, `danger`, `success`, `tip`, `note`, `example`, `experimental`, `deprecated`, `bug`, `performance`.

```md
:::tip[Reloading]
:cmd[/tw reload] picks up new extensions without a restart.
:::


:::warning
Facts are per player. Do not use them for global state.
:::
```

Reloading

/tw reload picks up new extensions without a restart.

Warning

Facts are per player. Do not use them for global state.

### All variants

[Section titled “All variants”](#all-variants)

Without a bracketed title, each variant uses its own default title.

```md
:::info
Something worth knowing.
:::
```

Info

General information the reader should have before continuing.

Warning

Something that can go wrong if the reader is not careful.

Danger

Something that destroys data or cannot be undone.

Success

What a correct result looks like.

Tip

A shortcut or a better way to do the same thing.

Note

A side remark that does not affect the main flow.

Example

```yaml
websocket:
  port: 9092
```

Experimental

Behaviour that may still change before the next release.

Deprecated

Still works, but has a replacement you should move to.

Known Issue

A known issue and its workaround.

Performance

Something that gets slow at scale and how to avoid it.

## Blocks

[Section titled “Blocks”](#blocks)

### TL;DR

[Section titled “TL;DR”](#tldr)

A summary box for the top of long pages. The label is optional.

```md
:::tldr
Facts store per-player numbers. Set them with actions, read them with criteria.
:::
```

TL;DR

Facts store per-player numbers. Set them with actions, read them with criteria.

### Collapsible

[Section titled “Collapsible”](#collapsible)

A native collapsible. `{open}` starts it expanded, `{id=anchor}` makes it linkable, and blocks sharing a `{name=group}` close each other, so only one is open at a time.

```md
:::details[Why does this happen?]
A player runs one interaction at a time. A new one only replaces the current one when its priority is equal or higher.
:::
```

Why does this happen?

A player runs one interaction at a time. A new one only replaces the current one when its priority is equal or higher.

### Gated section

[Section titled “Gated section”](#gated-section)

For content you would rather people did not use. The whole section stays blurred behind a card until the reader opts in. Variants: `warning` (default), `danger`, `info`, `neutral`; `{open}` starts revealed.

```md
:::spoiler[Not recommended]{reason="The next build overwrites this file." button="Show me anyway" variant=warning}
## Editing extension.json by hand


The extension processor generates it from your annotations.
:::
```

Not recommendedHide again

### Editing extension.json by hand

[Section titled “Editing extension.json by hand”](#editing-extensionjson-by-hand)

Change the `@Entry` annotations in your Kotlin sources instead. If you must inspect the file, it sits in the built JAR.

```json
{ "extension": { "name": "Basic", "namespace": "typewritermc", "engineVersion": "1.0.2" } }
```

Not recommended

The extension processor regenerates this file on every build, so hand edits disappear.

Show me anyway

Reveals 3 blocks, including 1 code block

## Interactive

[Section titled “Interactive”](#interactive)

### Decision wizard

[Section titled “Decision wizard”](#decision-wizard)

A nested list becomes a click-through questionnaire. The single top-level item is the root question, its children are answers, and `Answer → Follow-up question` items nest further. Items without children are results. `{persist}` remembers the reader’s path for the session.

```md
:::wizard[Which dialogue entry do I need?]
- Should the player see text?
  - Yes → Should they pick an option?
    - Yes → Use the **Option Dialogue** entry.
    - No → Use the **Spoken Dialogue** entry.
  - No → Look at the action entries instead.
:::
```

Which dialogue entry do I need?

* Should the player see text?

  * Yes → Should they pick an option?

    * Yes → Use the **Option Dialogue** entry.
    * No → Use the **Spoken Dialogue** entry.

  * No → Look at the action entries instead.

### Annotated screenshot

[Section titled “Annotated screenshot”](#annotated-screenshot)

Pins are placed by percentage. Clicking a pin opens its description; the legend list stays hidden unless the image cannot be shown. `{numbers=false}` gives dot pins, `{legend=visible}` always shows the list.

```md
:::hotspots[The page editor of a book]
![The panel](./panel.png)


1. [12%, 30%] **Pages** — every page in the book, grouped by chapter.
2. [50%, 45%] **Graph** — page elements are the nodes, triggers are the edges.
3. [86%, 20%] **Inspector** — fields of the selected element.
:::
```

![The panel](/branches/features/v1-docs/_astro/panel.CeUIBT4m_Z1CPCYT.webp)123

The page editor of a book

1. 1

   **Pages** — every page in the book, grouped by chapter.
2. 2

   **Graph** — page elements are the nodes, triggers are the edges.
3. 3

   **Inspector** — fields of the selected element.

### Before and after

[Section titled “Before and after”](#before-and-after)

Two images of the same size, split by a draggable handle. Options: `start` (percent), `orientation=vertical`, `hover`.

```md
:::compare[Before|After]{start=50}
![The old panel](./before.png)
![The new panel](./after.png)
:::
```

![The old panel](/branches/features/v1-docs/_astro/before.DW_wzOCz_ZjeLaC.webp)Before

![The new panel](/branches/features/v1-docs/_astro/after.BdQ7bdNp_Z17mM4E.webp)After

50

## Code

[Section titled “Code”](#code)

### Code blocks

[Section titled “Code blocks”](#code-blocks)

Fenced blocks support a title, line marks and diff marks.

````md
```yaml title="config.yml" {2} ins={4} del={3}
websocket:
  port: 9092
  host: 0.0.0.0
  hostname: play.example.com
```
````

config.yml

```diff
websocket:
  port: 9092
  -host: 0.0.0.0
  +hostname: play.example.com
```

### Server logs

[Section titled “Server logs”](#server-logs)

Use the `log` language for Paper output. Levels are coloured, stack traces are dimmed. Options: `title="latest.log"`, `collapse-traces`, `highlight="Typewriter"`, `wrap`.

````md
```log highlight="Typewriter"
[12:00:01 INFO]: [Typewriter] Loading 3 extensions
[12:00:01 WARN]: [Typewriter] Extension 'Quest' is outdated
[12:00:02 ERROR]: [Typewriter] Could not load extension 'Basic'
java.lang.IllegalStateException: Manifest missing
  at com.typewritermc.engine.ExtensionLoader.load(ExtensionLoader.kt:88)
```
````

```plaintext
1
[12:00:01 INFO]: [Typewriter] Loading 3 extensions
2
[12:00:01 WARN]: [Typewriter] Extension 'Quest' is outdated
3
[12:00:02 ERROR]: [Typewriter] Could not load extension 'Basic'
4
java.lang.IllegalStateException: Manifest missing
5
  at com.typewritermc.engine.ExtensionLoader.load(ExtensionLoader.kt:88)
```

## Components

[Section titled “Components”](#components)

These need an `.mdx` file and an import at the top.

### Steps

[Section titled “Steps”](#steps)

```mdx
import { Steps } from '@components/steps';


<Steps>


1. Download the jar.
2. Drop it in `plugins/`.
3. Restart the server.


</Steps>
```

1. Download the jar.
2. Drop it in `plugins/`.
3. Restart the server.

### Tabs

[Section titled “Tabs”](#tabs)

`syncKey` keeps the same tab selected across every group on the site that shares the key.

```mdx
import { Tabs, Tab } from '@components/tabs';


<Tabs syncKey="os">
  <Tab label="Windows">Press :kbd[Ctrl+L] to move to the next pane.</Tab>
  <Tab label="macOS">Press :kbd[Cmd+L] to move to the next pane.</Tab>
</Tabs>
```

Press `CtrlControl+L` to move to the next pane.

Press `⌘ CmdCommand+L` to move to the next pane.

### Video

[Section titled “Video”](#video)

Clips live in `src/assets/videos/`. They play muted and loop by default; pass `audio` for clips with sound.

```mdx
import { VideoPlayer } from '@components/video';


<VideoPlayer src="@assets/videos/examples/world-specific.webm" />
```

[](/branches/features/v1-docs/_astro/world-specific.BacefuuM.webm)

0:00 / 0:00

## Links and images

[Section titled “Links and images”](#links-and-images)

Link to other pages by relative file path, including the extension; the build rewrites it to the page URL. Images sit next to the page that uses them.

```md
See the [developer docs](../develop/index.mdx) and the [glossary](../glossary.mdx).


![The panel](./panel.png)
```

## Nesting directives

[Section titled “Nesting directives”](#nesting-directives)

A directive inside another directive needs the **outer** fence to use more colons, otherwise the inner closing fence ends the outer block.

```md
::::details[With an aside inside]
:::note
Four colons outside, three inside.
:::
::::
```

With an aside inside

Note

Four colons outside, three inside.

# Long Text

> A super long text to demonstrate the capabilities of the documentation system.

**Original answer:** I’m not sure if you will like how mathematical courses typically introduce matrices. As a programmer you might be happier with grabbing any decent 3D graphics book. It should certainly have very concrete 3x3 matrices. Also find out the ones that will teach you [projective transformations](http://en.wikipedia.org/wiki/Projective_transformations) (projective geometry is a very beautiful area of low-dimensional geometry and easy to program).

# Mini-course in matrix math with Python 3

[Section titled “Mini-course in matrix math with Python 3”](#mini-course-in-matrix-math-with-python-3)

**Contents:**

1. **Matrices** `[Vector, __add__, reflect_y, rotate, dilate, transform]`
2. **Matrices: Overloaded** `[Matrix, __add__, __str__, __mul__, zero, det, inv, __pow__]`
3. **Bonus: Complex numbers**
4. **Matrices: The (R)evolution**. It’s already in the making (there’s a summary at the end)

**Preface:** Based on my teaching experience, I think that the courses referenced by others are very good *courses*. That means if your goal is understanding matrices as mathematicians do, than you should by all means get the whole course. But if your goals are more modest, here’s my try at something more tailored to your needs (but still written with the goal to convey many theoretical concepts, kind of contradicting my original advice.)

**How to use:**

* This post is long. You might consider printing this and going slow, like one part a day.
* Code is essential. This is a course for programmers. Exercises are essential too.
* You should **[take a look at the code companion](http://mit.edu/~unknot/www/matrices.py)** which contains all this code and much more
* It’s “2 for the price of 1” special: you can also learn Python 3 here. And complex numbers.
* I’ll highly value any attempt to read this (do I officially qualify for the longest post ever?), so feel free to comment if you don’t understand something (and also if you do).

1. Matrices =

## Vectors

[Section titled “Vectors”](#vectors)

Before matrices come vectors. You sure know how to handle the 2- and 3- dimensional vectors:

class Vector: """This will be a simple 2-dimensional vector.

In case you never encountered Python before, this string is a comment I can put on the definition of the class or any function. It’s just one of many cool features of Python, so learn it here!

"""

def **init**(self, x, y): self.x = x self.y = y

now you can write

v = Vector(5, 3) w = Vector(7, -1)

but it’s not much fun by itself. Let’s add more useful methods:

def **str**(self: ‘vector’) -> ‘readable form of vector’: return ’(0, 1)‘.format(self.x, self.y)

def **add**(self:‘vector’, v: ‘another vector’) -> ‘their sum’: return Vector(self.x + v.x, self.y + v.y)

def **mul**(self:‘vector’, number: ‘a real number’) -> ‘vector’: '''Multiplies the vector by a number''' return Vector(self.x \* number, self.y \* number)

That makes things more interesting as we can now write:

print(v + w \* 2)

and get the answer `(19, 1)` nicely printed as a vector (if the examples look unfamiliar, think how this code would look in C++).

## Tranformations

[Section titled “Tranformations”](#tranformations)

Now it’s all cool to be able to write `1274 * w` but you need more vector operations for the graphics. Here are some of them: you can flip the vector around `(0,0)` point, you can reflect it around `x` or `y` axis, you can rotate it clockwise or counterclockwise (it’s a good idea to draw a picture here).

Let’s do some simple operations:

…

def flip(self:‘vector’) -> ‘vector flipped around 0’: return Vector(-self.x, -self.y)

def reflect\_x(self:‘vector’) -> ‘vector reflected around x axis’: return Vector(self.x, -self.y)

print(v.flip(), v.reflect\_x())

* **Question:** is it possible to express `flip(...)` using the operations I had below? What about `reflect_x`?

Now you may wonder why I omitted `reflect_y`. Well, it’s because I want you to stop for a moment and write your own version of it. Ok, here’s mine:

def reflect\_y(self:‘vector’) -> ‘vector reflected around y axis’: return self.flip().reflect\_x()

See, if you look how this function computes, it’s actually quite trivial. But suddenly an amazing thing happened: I was able to write a transformation using only the existing transformations `flip` and `reflect_x`. For all I care, `reflect_y` could be defined in a derived class without access to `x` and `y` and it would still work!

Mathematicians would call these functions *operators*. They would say that `reflect_y` is an operator obtained by *composition* of operators `flip` and `reflect_x` which is denoted by `reflect_y = flip ? reflect_x` (you should see the small circle, a Unicode symbol `25CB`).

* **Note:** I will quite freely use the `=` symbol from now to denote that two operations produce the same result, like in the paragraph above. This is a “mathematical `=`”, which [cannot be expressed as a program](http://en.wikipedia.org/wiki/List_of_undecidable_problems).

So if I do

print(v.reflect\_y())

I get the result `(-5, 3)`. Go and picture it!

* **Question:** Consider a composition `reflect_y ? reflect_y`. How would you name it?

## Rotations

[Section titled “Rotations”](#rotations)

Those operations were nice and useful, but you are probably wondering why am so slow to introduce rotations. Ok, here I go:

def rotate(self:‘vector’, angle:‘rotation angle’) -> ‘vector’: ??????

At this point, if you know how to rotate vectors, you should go on and fill in the question marks. Otherwise please bear with me for one more simple case: counterclockwise rotation by `90` degrees. This one is not hard to draw on a piece of paper:

def rotate\_90(self:‘vector’) -> ‘rotated vector’: new\_x = - self.y new\_y = self.x return Vector(new\_x, new\_y)

Trying

x\_axis = Vector(1, 0) y\_axis = Vector(0, 1)

print(x\_axis.rotate\_90(), y\_axis.rotate\_90())

now gives `(0, 1) (-1, 0)`. Run it yourself!

* **Question:** Prove that `flip = rotate_90 ? rotate_90`.

Anyway, I won’t hide the secret ingredient for longer:

import math # we’ll need math from now on …

class Vector:

…

def rotate(self:‘vector’, angle:‘rotation angle’) -> ‘rotated vector’: cos = math.cos(angle) sin = math.sin(angle) new\_x = cos \* self.x - sin \* self.y new\_y = sin \* self.x + cos \* self.y return Vector(new\_x, new\_y)

Now let’s try something along the lines:

print(x\_axis.rotate(90), y\_axis.rotate(90))

If you expect the same result as before, `(0, 1) (-1, 0)`, you’re bound to be disappointed. That code prints:

(-0.448073616129, 0.893996663601) (-0.893996663601, -0.448073616129)

and boy, is it ugly!

* **Notation:** I will say that we applied operation `rotate(90)` to `x` in the example above. The knowledge we gained is that `rotate(90) != rotate_90`.

* **Question:** What happened here? How to express `rotate_90` in terms of `rotate`? How to express `flip` in terms of `rotate`?

## Dilations

[Section titled “Dilations”](#dilations)

Those rotations are certainly useful, but they are not everything you need to do even the 2D graphics. Consider the following transformations:

def dilate(self:‘vector’, axe\_x:‘x dilation’, axe\_y:‘y dilation’): '''Dilates a vector along the x and y axes''' new\_x = axe\_x \* self.x new\_y = axe\_y \* self.y return Vector(new\_x, new\_y)

This `dilate` thing dilates the `x` and `y` axes in a possibly different way.

* **Exercise:** Fill in the question marks in `dilate(?, ?) = flip`, `dilate(?, ?) = reflect_x`.

I will use this `dilate` function to demonstrate a thing mathematicians call *commutativity*: that is, for every value of parameters `a`, `b`, `c`, `d` you can be sure that

dilate(a, b) ? dilate(c, d) = dilate(c, d) ? dilate(a, b)

* **Exercise:** Prove it. Also, is it true that for all possible values of parameters those below would hold?

  * `rotate(a) ? rotate(b) = rotate(b) ? rotate(a)`
  * `dilate(a, b) ? rotate(c) = rotate(c) ? dilate(a, b)`
  * `rotate(a) ? __mul__(b) = __mul__(b) ? rotate(a)`

## Matrices

[Section titled “Matrices”](#matrices)

Let’s summarize all the stuff we had around here, our *operators on vector `x`*

* `flip`, `reflect_x`, `*`, `rotate(angle)`, `dilate(x, y)`

from which one could make some really crazy stuff like

* `flip ? rotate(angle) ? dilate(x, y) ? rotate(angle_2) ? reflect_y + reflect_x = ???`

As you create more and more complicated expressions, one would hope for some kind of order that would suddenly reduce all possible expressions to a useful form. Fear not! Magically, every expression of the form above can be simplified to

def ???(self:‘vector’, parameters): '''A magical representation of a crazy function''' new\_x = ? \* self.x + ? \* self.y new\_y = ? \* self.x + ? \* self.y return Vector(new\_x, new\_y)

with some numbers and/or parameters instead of `?`s.

* **Example:** Work out what the values of ’?’ are for `__mul__(2) ? rotate(pi/4)`
* **Another example:** Same question for `dilate(x, y) ? rotate(pi/4)`

This allows us to write a universal function

def transform(self:‘vector’, m:‘matrix’) -> ‘new vector’: new\_x = m\[0] \* self.x + m[1](http://en.wikipedia.org/wiki/Projective_transformations) \* self.y new\_y = m[2](http://mit.edu/~unknot/www/matrices.py) \* self.x + m[3](http://en.wikipedia.org/wiki/List_of_undecidable_problems) \* self.y return Vector(new\_x, new\_y)

which would take any 4-tuple of numbers, called **matrix**, and *apply* it to vector `x`. Here’s an example:

rotation\_90\_matrix = (0, -1, 1, 0) print(v, v.rotate\_90(), v.transform(rotation\_90\_matrix))

which prints `(5, 3) (-3, 5) (-3, 5)`. Note that if you apply `transform` with any matrix to origin, you still get origin:

origin = Vector(0, 0) print(origin.transform(rotation\_90\_matrix))

* **Exercise:** what are the tuples `m` that describe `flip`, `dilate(x, y)`, `rotate(angle)`?

As we part with the `Vector` class, here’s an exercise for those who want to test both their vector math knowledge and Pythonic skills:

* **The final battle:** Add to the `Vector` class all vector operations that you can come up with (how many of standard operators can you overload for vectors? Check out my answer).

2. Matrices: Overloaded =

As we found out in the previous section, a matrix can be thought of a shorthand that allows us to encode a vector operation in a simple way. For example, `rotation_90_matrix` encodes the rotation by 90 degrees.

## Matrix objects

[Section titled “Matrix objects”](#matrix-objects)

Now as we shift our attention from vectors to matrices, we should by all means have a class for matrix as well. Moreover, in that function `Vector.transform(...)` above the role of the matrix was somewhat misrepresented. It’s more usual for `m` to be fixed while vector changes, so from now on our transformations will be methods of matrix class:

class Matrix:

def **init**(self:‘new matrix’, m:‘matrix data’): '''Create a new matrix.

So far a matrix for us is just a 4-tuple, but the action will get hotter once The (R)evolution happens!

''' self.m = m

def **call**(self:‘matrix’, v:‘vector’): new\_x = self.m\[0] \* v.x + self.m[1](http://en.wikipedia.org/wiki/Projective_transformations) \* v.y new\_y = self.m[2](http://mit.edu/~unknot/www/matrices.py) \* v.x + self.m[3](http://en.wikipedia.org/wiki/List_of_undecidable_problems) \* v.y return Vector(new\_x, new\_y)

If you don’t know Python, `__call__` overloads the meaning of `(...)` for matrices so I can use the standard notation for a matrix *acting* on a vector. Also, the matrices are usually written using a single uppercase letter:

J = Matrix(rotation\_90\_matrix) print(w, ‘rotated is’, J(w))

* **Exercise:** repeat this example with matrices from the previous exercise.

## Addition

[Section titled “Addition”](#addition)

Now, let’s find out what else we can do with matrices. Remember that matrix `m` is really just a way to encode an operaton on vectors. Note that for two functions `m1(x)` and `m2(x)` I can create a new function (using [lambda notation](http://en.wikipedia.org/wiki/Lambda_calculus)) `m = lambda x: m1(x) + m2(x)`. It turns out if `m1` and `m2` were enconded by matrices, *you can also encode this `m` using matrices*!

* **Exercise:** Think through any difficulties you might have with this statement.

You just have to add its data, like `(0, 1, -1, 0) + (0, 1, -1, 0) = (0, 2, -2, 0)`. Here’s how to add two tuples in Python, with some very useful and highly Pythonic techniques:

def **add**(self:‘matrix’, snd:‘another matrix’): """This will add two matrix arguments.

snd is a standard notation for second argument. (i for i in array) is Python’s powerful list comprehension. zip(a, b) is used to iterate over two sequences together

"""

new\_m = tuple(i + j for i, j in zip(self.m, snd.m)) return Matrix(new\_m)

Now we can write expressions like `J + J` or even `J + J + J`, but to see the results we have to figure out how to print a Matrix. A possible way would be to print a 4-tuple of numbers, but let’s take a hint from the `Matrix.__call__` function that the numbers should be organized into a `2x2` block:

def as\_block(self:‘matrix’) -> ‘2-line string’: """Prints the matrix as a 2x2 block.

This function is a simple one without any advanced formatting. Writing a better one is an exercise.

"""

return (’| 0 1 |\n’ .format(self.m\[0], self.m[1](http://en.wikipedia.org/wiki/Projective_transformations)) + ’| 0 1 |\n’ .format(self.m[2](http://mit.edu/~unknot/www/matrices.py), self.m[3](http://en.wikipedia.org/wiki/List_of_undecidable_problems)) )

If you look at this function in action you’ll notice there is some room for improvement:

print((J + J + J).as\_block())

* **Exercise:** write a nicer function `Matrix.__str__` that will round the numbers and print them in the fields of fixed length.

Now you should be able to write the matrix for rotation:

def R(a: ‘angle’) -> ‘matrix of rotation by a’: cos = math.cos(a) sin = math.sin(a) m = ( ????? ) return Matrix(m)

* **Exercise:** Examine the code for `Vector.rotate(self, angle)` and fill in the question marks. Test with

  from math import pi\
  print(R(pi/4) + R(-pi/4))

## Multiplication

[Section titled “Multiplication”](#multiplication)

The most important thing we can do with one-parameter functions is compose them: `f = lambda v: f1(f2(v))`. How to mirror that with matrices? This requires us to examine how `Matrix(m1) ( Matrix(m2) (v))` works. If you expand it, you’ll notice that

m(v).x = m1\[0] \* (m2\[0]\*v.x + m2[1](http://en.wikipedia.org/wiki/Projective_transformations)\*v.y) + m1[1](http://en.wikipedia.org/wiki/Projective_transformations) \* (m2[2](http://mit.edu/~unknot/www/matrices.py)\*v.x + m2[3](http://en.wikipedia.org/wiki/List_of_undecidable_problems)\*v.y)

and similarly for `m(v).y`, which, if you open the parentheses, looks suspiciously similar to `Matrix.__call__` using a new tuple `m`, such that `m[0] = m1[0] * m2[0] + m1[2] * m2[2]`. So let’s take this as a hint for a new definiton:

def compose(self:‘matrix’, snd:‘another matrix’): """Returns a matrix that corresponds to composition of operators"""

new\_m = (self.m\[0] \* snd.m\[0] + self.m[1](http://en.wikipedia.org/wiki/Projective_transformations) \* snd.m[2](http://mit.edu/~unknot/www/matrices.py), self.m\[0] \* snd.m[1](http://en.wikipedia.org/wiki/Projective_transformations) + self.m[1](http://en.wikipedia.org/wiki/Projective_transformations) \* snd.m[3](http://en.wikipedia.org/wiki/List_of_undecidable_problems), ???, ???) return Matrix(new\_m)

* **Exercise:** Fill in the question marks here. Test it with

  print(R(1).compose(R(2))) print(R(3))

* **Math exercise:** Prove that `R(a).compose(R(b))` is always the same as `R(a + b)`.

Now let me tell the truth: this `compose` function is actually how mathematicians decided to *multiply* matrices. This makes sense as a notation: `A * B` is a matrix that decribes operator `A ? B`, and as we’ll see next there are deeper reasons to call this ‘multiplication’ as well.

To start using multiplication in Python all we have to do is to order it so in a `Matrix` class:

class Matrix:

…

**mul** = compose

* **Exercise:** Compute `(R(pi/2) + R(pi)) * (R(-pi/2) + R(pi))`. Try to find the answer yourself first on a piece of paper.

## Rules for `+` and `*`

[Section titled “Rules for + and \*”](#rules-for--and-)

Let’s make some good name for the matrix that corresponds to the `dilate(a, b)` operator. Now there’s nothing wrong with `D(a, b)`, but I’ll use a chance to introduce a standard notation:

def diag(a: ‘number’, b: ‘number’) -> ‘diagonal 2x2 matrix’: m = (a, 0, 0, b) return Matrix(m)

Try `print(diag(2, 12345))` to see why it’s called a *diagonal* matrix.

As the composition of operations was found before to be not always commutative, `*` operator won’t be always commutative for matrices either.

* **Exercise:** go back and refresh the *commutativity* thing if necessary. Then give examples of matrices `A`, `B`, made from `R` and `diag`, such that `A * B` is not equal to `B * A`.

This is somewhat strange, since multiplication for numbers is always commutative, and raises the question whether `compose` really deserves to be called `__mul__`. Here’s quite a lot of rules that `+` and `*` *do* satisfy:

1. `A + B = B + A`
2. `A * (B + C) = A * B + A * C`
3. `(A + B) * C = A * C + B * C`
4. `(A * B) * C = A * (B * C)`
5. There is an operation called `A - B` and `(A - B) + B = A`

* **Exercise:** Prove these statements. How to define `A - B` in terms of `+`, `*` and `diag`? What does `A - A` equal to? Add the method `__sub__` to the class `Matrix`. What happens if you compute `R(2) - R(1)*R(1)`? What should it be equal to?

The `(A * B) * C = A * (B * C)` equality is called *associativity* and is especially nice since it means that we don’t have to worry about putting parentheses in an expression of the form `A * B * C`:

print(R(1) \* (diag(2,3) \* R(2))) print((R(1) \* diag(2,3)) \* R(2))

Let’s find analogues to regular numbers `0` and `1` and subtraction:

zero = diag(0, 0) one = diag(1, 1)

With the following easily verifiable additions:

1. `A + zero = A`
2. `A * zero = zero`
3. `A * one = one * A = A`

the rules become complete, in the sense that there is a short name for them: *ring axioms*. Mathematicians thus would say that matrices form a *ring*, and they indeed always use symbols `+` and `*` when talking about rings, and so shall we.

Using the rules it’s possible to easily compute the expression from the previous section:

(R(pi/2) + R(pi)) \* (R(-pi/2) + R(pi)) = R(pi/2) \* R(-pi/2) + … = one + …

* **Exercise:** Finish this. Prove that `(R(a) + R(b)) * (R(a) - R(b)) = R(2a) - R(2b)`.

## Affine Transformations

[Section titled “Affine Transformations”](#affine-transformations)

Time to return to how we defined matrices: they are a shortcut to some operations you can do with vectors, so it’s something you can actually draw. You might want to take a pen or look at the materials that others suggested to see examples of different plane transformations.

Among the transformations we’ll be looking for the *affine* ones, those who look ‘the same’ everywhere (no bending). For example, a rotation around some point `(x, y)` qualifies. Now this one cannot be expressed as `lambda v: A(v)`, but in can be written in the form `lambda v: A(v) + b` for some matrix `A` and vector `b`.

* **Exercise:** find the `A` and `b` such that a rotation by `pi/2` around the point `(1, 0)` has the form above. Are they unique?

Note that for every vector there is an affine transformation which is a *shift* by the vector.

An affine transformation may stretch or dilate shapes, but it should do in the same way everywhere. Now I hope you believe that the area of any figure changes by a constant number under the transformation. For a transformation given by matrix `A` this coeffiecient is called the *determinant* of `A` and can be computed applying the formula for an area to two vectors `A(x_axis)` and `A(y_axis)`:

def det(self: ‘matrix’) -> ‘determinant of a matrix’: return self.m\[0]\*self.m[3](http://en.wikipedia.org/wiki/List_of_undecidable_problems) - self.m[1](http://en.wikipedia.org/wiki/Projective_transformations) \* self.m[2](http://mit.edu/~unknot/www/matrices.py)

As a sanity check, `diag(a, b).det()` is equal to `a * b`.

* **Exercise:** Check this. What happens when one of arguments is 0? When it’s negative?

As you can see, the determinant of rotation matrix is always the same:

from random import random r = R(random()) print (r, ‘det =’, r.det())

One interesting thing about `det` is that it is multiplicative (it kind of follows from the definition if you meditate long enough):

A = Matrix((1, 2, -3, 0)) B = Matrix((4, 1, 1, 2)) print(A.det(), ’\*’, B.det(), ‘should be’, (A \* B).det())

## Inverse

[Section titled “Inverse”](#inverse)

A useful thing you can do with matrices is write a system of two linear equations

A.m\[0]\*v.x + A.m[1](http://en.wikipedia.org/wiki/Projective_transformations)\*v.y = b.x A.m[2](http://mit.edu/~unknot/www/matrices.py)\*v.x + A.m[3](http://en.wikipedia.org/wiki/List_of_undecidable_problems)\*v.y = b.y

in a simpler way: `A(v) = b`. Let’s solve the system as they teach in (some) high schools: multiply first equation by `A.m[3]`, second by -A.m[1](http://en.wikipedia.org/wiki/Projective_transformations) and add (if in doubt, do this on a piece of paper) to solve for `v.x`.

If you really tried it, you should have got `A.det() * v.x = (A.m[3]) * b.x + (-A.m[1]) * b.y`, which suggests that you can always get `v` by multiplying `b` by some other matrix. This matrix is called *inverse* of `A`:

def inv(self: ‘matrix’) -> ‘inverse matrix’: '''This function returns an inverse matrix when it exists, or raises ZeroDivisionError when it doesn’t.

''' new\_m = ( self.m[3](http://en.wikipedia.org/wiki/List_of_undecidable_problems) / self.det(), -self.m[1](http://en.wikipedia.org/wiki/Projective_transformations) / self.det(), ????? ) return Matrix(new\_m)

As you see, this method fails loudly when determinant of matrix is zero. If you really want you can catch this expection with:

try: print(zero.inv()) except ZeroDivisionError as e: …

* **Exercise:** Finish the method. Prove that inverse matrix doesn’t exist when `self.det() == 0`. Write the method to divide matrices and test it. Use the inverse matrix to solve an equation `A(v) = x_axis` (`A` was defined above).

## Powers

[Section titled “Powers”](#powers)

The main property of inverse matrix is that `A * A.inv()` always equals to `one`

* **Exercise:** check that yourself. Explain why that should be so from the definition of inverse matrix.

That’s why mathematicians denote `A.inv()` by `A`-1. How about we write a nice function to use `A ** n` notation for `A`n? Note that the naive `for i in range(n): answer *= self` cycle is O(|n|) which is certainly too slow, because this can be done with a complexity of `log |n|`:

def **pow**(self: ‘matrix’, n:‘integer’) -> ‘n-th power’: '''This function returns n-th power of the matrix.

It does it more efficiently than a simple for cycle. A while loop goes over all bits of n, multiplying answer by self \*\* (2 \*\* k) whenever it encounters a set bit.

…

* **Exercise:** Fill in the details in this function. Test it with

  `X, Y = A ** 5, A ** -5` `print (X, Y, X * Y, sep = '\n')`

This function only works for integer values of `n`, even though for some matrices we can also define a fractional power, such as square root (in other words, a matrix `B` such that `B * B = A`).

* **Exercise:** Find a square root of `diag(-1, -1)`. Is this the only possible answer? Find an example of matrix that *doesn’t* have a square root.

# Bonus: Complex numbers

[Section titled “Bonus: Complex numbers”](#bonus-complex-numbers)

Here I’m going to introduce you to the subject in exactly one section! Since it’s a complex subject, I’m likely to fail, so please forgive me in advance.

First, similarly to how we have matrices `zero` and `one`, we can make a matrix out of any real number by doing `diag(number, number)`. Matrices of that form can be added, subtracted, multiplied, inverted and the results would mimic what happens with the numbers themselves. So for all practical purposes, one can say that, e.g., `diag(5, 5)` *is* 5.

However, Python doesn’t know yet how to handle expressions of the form `A + 1` or `5 * B` where `A` and `B` are matrices. If you’re interested, you should by all means go and do the following exercise or look at my implementation (which uses a cool Python feature called *decorator*); otherwise, just know that it’s been implemented.

* **Exercise for gurus:** Change the operators in a `Matrix` class so that in all standard operations where one of operands is a matrix and another a number, the number is automatically converted to the `diag` matrix. Also add comparison for equality.

Here’s an example test:

print( 3 \* A - B / 2 + 5 )

Now here’s the first interesting **complex number**: the matrix `J`, introduced in the beginning and equal to `Matrix((0, 1, -1, 0))`, has a funny property that `J * J == -1` (try it!). That means `J` is certainly not a normal number, but, as I just said, matrices and numbers easily mix together. For example,

(1 + J) \* (2 + J) == 2 + 2 \* J + 1 \* J + J \* J = 1 + 3 \* J

using the rules listed some time before. What happens if we test this in Python?

(1 + J) \* (2 + J) == 1 + 3\*J

That should happily say `True`. Another example:

(3 + 4*J) / (1 - 2*J) == -1 + 2\*J

As you might have guessed, the mathematicians don’t call those ‘crazy numbers’, but they do something similar - they call expressions of the form `a + b*J` *complex numbers*. Because those are still instances of our `Matrix` class, we can do quite a lot of operations with those: addition, subtraction, multiplication, division, power - it’s all already implemented! Aren’t matrices amazing?

I have overlooked the question of how to print the result of operation like `E = (1 + 2*J) * (1 + 3*J)` so that it looks like an expression with `J` rather than a `2x2` matrix. If you examine it carefully, you’ll see that you need to print the left column of that matrix in the format `... + ...J` (just one more nice thing: it’s exactly `E(x_axis)`!) Those who know the difference between `str()` and `repr()` should see it’s natural to name a function that would produce expression of such form as `repr()`.

* **Exercise:** Write the function `Matrix.__repr__` that would do exactly that and try some tests with it, like `(1 + J) ** 3`, first computing the result on paper and then trying it with Python.

* **Math question:** What is the determinant of `a + b*J`? If you know what the *absolute value* of complex number is: how they are connected? What is the absolute value of `a`? of `a*J`?

3. Matrices: The (R)evolution =

In the final part of this trilogy we will see that everything is a matrix. We’ll start with general `M x N` matrices, and find out how vectors can be thought of as `1 x N` matrices and why numbers are the same as diagonal matrices. As a side note we’ll explore the complex numbers as `2 x 2` matrices.

Finally, we will learn to write affine and projective transformations using matrices.

So the classes planned are `[MNMatrix, NVector, Affine, Projective]`.

I guess if you was able to bear with me until here, you could be interested in this sequel, so I’d like to hear if I should continue with this (and where, since I’m pretty much sure I’m beyond what considered reasonable length of a single document).

# Glossary

> Definitions of terms used throughout the Typewriter documentation.

Hover any dotted term in the documentation to see its definition, or browse all terms below.

## Audience

`audience``audiences`

An **audience** is the set of players a [manifest](/branches/features/v1-docs/glossary/#manifest) entry currently applies to. Display entries show their content to their audience; filter entries narrow the audience passed on to their children.

## Book

`book``books`

A **book** is a collection of [pages](/branches/features/v1-docs/glossary/#page) inside a [realm](/branches/features/v1-docs/glossary/#realm). Books are listed in the panel’s Library and each has a title, an icon, a colour and tags.

## Criteria

`criteria``criterion`

**Criteria** are the conditions on an [entry](/branches/features/v1-docs/glossary/#entry): a [fact](/branches/features/v1-docs/glossary/#fact), an operator and a value. Every criterion on an entry must pass before the entry runs — and if one fails, the entry’s [modifiers](/branches/features/v1-docs/glossary/#modifier) do not run either.

## Cue

`cue``cues``segment``segments``keyframe``keyframes`

A **cue** is a block on a [scene](/branches/features/v1-docs/glossary/#scene)’s [track](/branches/features/v1-docs/glossary/#track): either a *segment* with a start and end frame, or a *keyframe* at a single frame. Cues on one track cannot overlap. 20 frames make one second.

## Entry

`entry``entries`

An **entry** is the smallest building block of Typewriter content: a dialogue, an action, an event, a scene cue, each provided by an [extension](/branches/features/v1-docs/glossary/#extension).

* **Static entries** define reusable data, like NPC definitions.
* **Triggerable entries** run as part of an [interaction](/branches/features/v1-docs/glossary/#interaction).
* **Manifest entries** stay active for an [audience](/branches/features/v1-docs/glossary/#audience).

## Extension

`extension``extensions`

An **extension** is a modular add-on that provides new entries to Typewriter. Extensions are `.jar` files placed in the `plugins/Typewriter/extensions` folder. The `BasicExtension` ships the essential entries most servers need.

## Fact

`fact``facts`

A **fact** is a number Typewriter remembers about a player — quest progress, a choice they made, how many of an item they carry. Every fact is an integer, read by [criteria](/branches/features/v1-docs/glossary/#criteria) and written by [modifiers](/branches/features/v1-docs/glossary/#modifier).

## Group entry

`group entry``group entries`

A **group entry** decides who shares a [fact](/branches/features/v1-docs/glossary/#fact)’s value: each player individually, everyone in a world, everyone on the server, or a grouping an extension defines such as a region or a party.

## Inspector

`inspector`

The **inspector** is the panel pane where you edit the selected [page element](/branches/features/v1-docs/glossary/#page-element): its name, its fields and the operations available on it.

## Interaction

`interaction``interactions`

An **interaction** is what a player experiences in game: a dialogue, quest or [scene](/branches/features/v1-docs/glossary/#scene) composed of connected entries. A player runs one interaction at a time; it starts when a [trigger](/branches/features/v1-docs/glossary/#trigger) fires and follows the flow you build on the page.

## Manifest

`manifest``manifests`

A **manifest** is a [page](/branches/features/v1-docs/glossary/#page) type whose entries describe state that should hold *while* a condition is true, instead of running once: [audiences](/branches/features/v1-docs/glossary/#audience), displays and filters.

## Modifier

`modifier``modifiers`

A **modifier** changes a [fact](/branches/features/v1-docs/glossary/#fact) after an [entry](/branches/features/v1-docs/glossary/#entry) has run. It sets, adds to, subtracts from or multiplies the stored value.

## Organization

`organization``organizations`

An **organization** is the top level of the panel. It has members, roles and join codes, and owns [realms](/branches/features/v1-docs/glossary/#realm) and the services connected to them.

## Page

`page``pages`

A **page** holds [page elements](/branches/features/v1-docs/glossary/#page-element) inside a [book](/branches/features/v1-docs/glossary/#book). Every page has a type — [Sequence](/branches/features/v1-docs/glossary/#sequence), [Static](/branches/features/v1-docs/glossary/#static), [Scene](/branches/features/v1-docs/glossary/#scene) or [Manifest](/branches/features/v1-docs/glossary/#manifest) — which decides what it can hold and how the elements connect, plus a chapter and a priority.

## Page element

`page element``page elements`

A **page element** is anything placed on a [page](/branches/features/v1-docs/glossary/#page): an [entry](/branches/features/v1-docs/glossary/#entry), a group of entries, or, on a [scene](/branches/features/v1-docs/glossary/#scene) page, a [cue](/branches/features/v1-docs/glossary/#cue). Elements are the nodes you see on the graph and the tracks you see on the timeline.

## Realm

`realm``realms`

A **realm** is the content space a Minecraft server connects to. Realms belong to an [organization](/branches/features/v1-docs/glossary/#organization) and hold [books](/branches/features/v1-docs/glossary/#book). A server joins a realm with the registration token printed in its console.

## Road network

`road network``road networks`

A **road network** is a graph of nodes and edges Typewriter uses to path efficiently between two distant points, so NPCs can walk sensible routes across your map instead of computing paths block by block.

## Scene

`scene``scenes``cinematic``cinematics`

A **scene** is a [page](/branches/features/v1-docs/glossary/#page) type for timed content: camera paths, animations and cutscenes built from [cues](/branches/features/v1-docs/glossary/#cue) placed on [tracks](/branches/features/v1-docs/glossary/#track). In extension code the scene family is still called `cinematic`.

## Sequence

`sequence``sequences`

A **sequence** is a [page](/branches/features/v1-docs/glossary/#page) type whose entries play one after another as an interaction unfolds. Most dialogues and quests are built on sequence pages.

## Static

`static`

A **static** page holds entries that reference data which doesn’t change over the lifetime of the program, such as NPC definitions or item templates.

## Track

`track``tracks`

A **track** is the timeline row of one entry on a [scene](/branches/features/v1-docs/glossary/#scene) page, where its [cues](/branches/features/v1-docs/glossary/#cue) are placed and timed. Time is measured in frames: 20 frames equal 1 second.

## Trigger

`trigger``triggers`

A **trigger** is the link between two [entries](/branches/features/v1-docs/glossary/#entry): when one finishes, the entries it triggers run next. Chaining triggers on a [sequence](/branches/features/v1-docs/glossary/#sequence) page is how an interaction plays out step by step.