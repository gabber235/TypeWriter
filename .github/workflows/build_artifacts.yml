name: Build Artifacts

on:
  push:
#    branches:
#      - master

jobs:
  build-app-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v1
        with:
          channel: 'stable'
      - name: Install dependencies
        working-directory: ./app
        run: flutter pub get
      - name: Build AppImage
        working-directory: ./app
        run: flutter build linux --release
      - name: Upload AppImage
        uses: actions/upload-artifact@v2
        with:
          name: Typewriter.AppImage
          path: ./app/build/linux/release/bundle/*.AppImage

  build-app-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - name: Install dependencies
        working-directory: ./app
        run: flutter pub get
      - name: Build Windows
        working-directory: ./app
        run: flutter build windows --release
      - name: Upload Windows
        uses: actions/upload-artifact@v2
        with:
          name: Typewriter.exe
          path: ./app/build/windows/release/runner/*.exe

  build-app-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - name: Install dependencies
        working-directory: ./app
        run: flutter pub get
      - name: Build MacOS
        working-directory: ./app
        run: flutter build macos --release
      - name: Upload MacOS
        uses: actions/upload-artifact@v2
        with:
          name: Typewriter.app
          path: ./app/build/macos/Build/Products/Release/*.app
