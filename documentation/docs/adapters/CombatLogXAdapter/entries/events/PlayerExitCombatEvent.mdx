import {EventsField, EntryField} from "@site/src/components/EntryField";

# Player Exit Combat Event

The `Player Exit Combat Event` is fired when a player exits combat.

## How could this be used?

This event could be used to trigger a custom action when a player exits combat.

## Fields

<EventsField />
