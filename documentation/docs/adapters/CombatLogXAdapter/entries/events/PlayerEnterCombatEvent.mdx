import {EventsField, EntryField} from "@site/src/components/EntryField";

# Player Enter Combat Event

The `Player Enter Combat Event` is triggered when a player enters combat with another player.

## How could this be used?

This event could be used to trigger a custom action when a player enters combat with another player.

## Fields

<EventsField />

<EntryField name="Aggressor Triggers" required>
    The triggers for the aggressor
</EntryField>
