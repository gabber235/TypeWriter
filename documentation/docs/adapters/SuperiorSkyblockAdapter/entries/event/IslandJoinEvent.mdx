import {EventsField, EntryField} from "@site/src/components/EntryField";

# Island Join Event

The `Island Join Event` is fired when a player joins an island.

## How could this be used?

This event could be used to give the player a welcome message when they join an island, or notify other members when they have joined.

## Fields

<EventsField />
