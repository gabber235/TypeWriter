import {EventsField, EntryField} from "@site/src/components/EntryField";

# Island Disband Event

The `Island Disband Event` is triggered when an island is disbanded.

## How could this be used?

This event could be used to remove the island, and prompt the user to make another one.

## Fields

<EventsField />
